# Dependencies
node_modules

# Build output
dist/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*
.next/
.vercel

# MCP config (contains secrets)
.mcp.json

# Claude Code 로컬 상태 (메모리/워크트리)
.claude/

# Supabase CLI 로컬 캐시
supabase/.temp/

# Codex CLI 로컬 설정
.codex/

# Overpass 원본 스냅샷(도시 geo 재현 전용·43M) — world-data 브랜치에 분리 보관
# (npm run world:fetch-data). Tatoeba 프랑스어 스냅샷은 260KB이고 일상 게이트가
# 직접 읽으므로 예외로 남긴다.
scripts/data/
!scripts/data/tatoeba-*.json
