# 설명판 전체 화면·아이콘 메뉴

2026-09-14 · `codex/teaching-board-immersive-20260914`

## 목표와 화면 구성

설명판을 열면 캔버스가 화면 전체를 사용한다. 웹앱 공통 헤더, 상시 입력줄, 하단 보관·페이지 줄이 차지하던 공간을 판으로 돌려준다. 기본으로 작은 아이콘 막대만 판 안에 떠 있고, 누른 기능의 메뉴 하나가 열린다. 메뉴를 열어도 캔버스 크기와 필기 좌표가 변하지 않는다.

| 기본 아이콘 | 클릭했을 때 | 보존하는 기능 |
| --- | --- | --- |
| 전체 메뉴 | 판과 도구 / 수업과 이동 / 백업 | 추가 도구, 페이지, 발표, 맞춤, 홈, 기록, 백업·복구, 닫기 |
| 선택 | 선택 모드로 바로 전환 | 요소 이동·선택·그룹 |
| 펜 | 필기로 바로 전환 | 현재 색·굵기, 기존 필기 엔진 |
| 지우개 | 지우개로 바로 전환 | 기존 요소 단위 지우기 |
| ＋ | 표현 입력 | 즉시 놓기, 교재/내 단어/최근 판 검색, 사전, 독음·뜻, 일본어 가나 후보 |
| 책 | 교재와 화면 | 설명판 전체/교재와 함께/교재 전체, 해당 수업 교재 과 선택 |

사용 중인 선택·펜·지우개 아이콘을 한 번 더 누르면 설정이 열린다. 기본 막대는 여섯 아이콘을 유지하며, 자주 반복하는 필기 전환을 2회에서 1회 클릭으로 줄였다. 페이지·발표는 전체 메뉴로 들어가므로 한 단계가 더 필요하다. 현재 수업 중 빈번한 필기 전환을 우선한 선택이다.

선택한 요소가 있을 때만 오른쪽 아래에 편집 아이콘이 나타난다. 해당 메뉴에서 내용·독음·뜻, 그룹, 명시적 오늘 표현 추가를 처리한다. 학생 수업 기록은 판에서 표시했다는 이유만으로 자동 생성하지 않는다.

## 동작과 접근성

- 화면·입력 크기를 줄이는 고정 상하단 줄을 제거하고 44px 이상의 아이콘 누름 영역을 둔다.
- 아이콘에 접근성 이름과 도구 설명을 제공하고 현재 메뉴/도구의 선택 상태를 표시한다.
- 같은 아이콘으로 닫기, 바깥 클릭으로 닫기, Escape로 닫기. 키보드 종료 시 연 아이콘으로 초점을 돌린다. 아이콘 막대는 방향키와 Home/End도 지원한다.
- 입력 메뉴를 접거나 다른 메뉴로 이동해도 작성 중인 표현을 유지한다. 기존 표현 수정과 새 표현 초안은 구분한다.
- 새 작업 공간은 설명판 전체 화면에서 시작한다. 이미 저장된 분할/교재 보기·분할 비율·입력 초안은 그대로 복원한다.
- 수업 기록과 저장 실패 재시도도 판 안에서 연다. 기록 수정/분석 실패/범위 확인 기능은 기존 수업 로직을 재사용한다.
- 스마트폰에서는 교재와 함께 보기를 위아래로 배치한다. 입력 시 줄어드는 화면 높이와 안전 여백을 적용한다.
- 보여주기 화면도 전체 판 위에 작은 아이콘을 띄운다. 발표용 임시 가리기·레이저는 원래 판을 바꾸지 않는다.

## 구현 경계

- Excalidraw 0.18.1 고정 버전 유지. 캔버스·필기·선택·그룹·텍스트·history는 기존 엔진을 사용한다.
- `BoardHistory`는 엔진이 실제로 렌더한 undo/redo 버튼의 동작과 disabled 상태를 전달한다. 엔진에 공개 undo/redo 메서드가 없어 고정 버전의 `button-undo`/`button-redo` 선택자를 좁게 사용한다. 브라우저 회귀 검사에서 실제 잉크의 취소·복구를 검증해야 한다. 패키지 업그레이드 시 확인할 경계다.
- 원문, 저장 형식, 개인 권한, 서버 수업 기록, FSRS, PDF, 음성, 운영 DB/환경은 변경하지 않는다. 서버 동기화·필기 인식은 추가하지 않는다.
- 기존 로컬 후속 `8c37142a53e0fbbee85b1a5e1e6091452cb76266`의 단어 확대 창 초점 복귀 보완도 포함한다.
- 원본 iCloud 작업 공간과 다른 PR의 변경은 보존한다. 별도 clone에서 진행한다.

## 최종 실행 코드 검증

실행 커밋: `138bd66386c3ccbef9d645510ce367ff66fbd8db`.

- 전체 단위 검사 390파일 / 4,201개 통과(46.42초). 빠른 도구 판단·기존 작업 공간 설정·초안 보존을 포함한다.
- 변경 JSX/JS를 명시적으로 포함한 ESLint, 로컬 모듈 37개 번들 검사와 CSS 구문 검사 통과.
- 실제 React 화면 + 합성 계정/API + 임시 PostgreSQL로 Chromium 20개, WebKit 터치 20개 흐름 통과. 양쪽 런타임 오류 0개. 운영 개인 자료에는 쓰지 않았다.
- 도구 한 번 전환/재클릭 설정, 354/390px·데스크톱의 판 크기 유지, 하위 메뉴 초점 복귀, 빈 안내 소거를 이번 코드에서 확인했다.
- 교재 표현/출처 가져오기, 필기, native undo/redo, 그룹 이동·새로고침, 발표·단어 확대, 수업 기록의 명시적 추가, 교재/과 전환, 초안·분할 비율, 키보드 높이·확대, 저장 충돌·복구·백업을 검증했다.
- 첫 구현의 별도 뷰어 디자인 검사는 Chromium 6개·WebKit 6개가 통과했다. 이를 이번 추가분의 재검사 수에 합산하지 않는다.
- CI 34831023820의 두 필수 job 모두 SUCCESS(lint·콘텐츠·전체 단위 검사, Next 빌드·조판·기존 학습 흐름). Vercel 실제 글꼴 포함 배포용 빌드도 완료됐으며 배포 버전 확인을 진행 중이다.

검수 이미지는 이 작업의 `board-quick-tools-20260914/{chromium,webkit}` 시각 자료 폴더에 보관했다. 합성 자료만 포함한다. 물리 iPad/Apple Pencil의 압력·팜 리젝션·회전과 실제 소프트웨어 입력기는 브라우저 터치·크기 모사와 구별하며 아직 검수하지 않았다.

## 배포와 인계

- PR: https://github.com/wonchance-art/manabi/pull/1311 . 실행 커밋 `138bd66386c3ccbef9d645510ce367ff66fbd8db`까지 공개 push 완료.
- 미리보기 빌드: https://manabi-447anhfbo-wonchance-arts-projects.vercel.app . 완료·버전 검수 전에는 운영 반영으로 보고하지 않는다.
- 최초 외부 요청은 자동 승인 검토 서버의 모델 용량 부족으로 간헐적으로 거부됐다. 이후 같은 승인된 요청으로 Chrome 검수·push·미리보기 배포가 정상 실행됐다. 사용자 승인 부족은 아니다.
- 최신 main은 `e29d41a7a7650e20e8984c2086d2b8b49f52b379`이며 이번 개선은 아직 병합하지 않았다. 필수 검사와 미리보기 검수 후 최신 head를 지정해 승인된 squash 병합·운영 배포를 진행한다. 최종 커밋·배포 상태는 PR 및 #150 인계에 기록한다.

## 평가 후 빠른 조작 개선

오너의 “점수 더 올릴 수 있도록 만들어봐” 요청에 따라 같은 PR 범위에서 추가했다.

- 선택/펜/지우개를 기본 도구막대로 이동. 첫 클릭은 바로 전환, 사용 중인 도구 재클릭은 설정 열기/닫기다. 교재 전체 보기에서는 첫 클릭이 설정을 열지 않고 사용할 수 있는 판으로 복귀한다.
- 펜 아이콘은 현재 색을 보여주며 새 판에서도 이번 작업 중 선택한 색·굵기를 유지한다. 저장된 교재와 기존 필기 요소의 색은 다시 칠하지 않는다.
- 지우개·레이저·화면 이동에는 무효한 색·굵기 설정을 표시하지 않는다. 지우개가 선/요소 단위로 작동함을 짧게 안내한다.
- 전체 메뉴를 판/수업/백업으로 분류하고 빠른 도구와 중복된 항목을 제거한다. 하위 메뉴를 닫을 때 숨겨진 버튼이 아니라 기본 막대의 보이는 버튼으로 초점을 돌린다.
- 빈 판에만 시작 안내를 표시한다. 글이나 필기 요소가 생기면 안내를 제거한다.

### 채택 근거

| 기존 구현 | 판단 | 적용 |
| --- | --- | --- |
| 설치된 Excalidraw 0.18.1의 직접 도구 선택과 `setActiveTool` | 부분 채택 | 빠른 선택/펜/지우개를 같은 엔진에 연결. 별도 필기·지우기 엔진 없음 |
| 기존 manabi의 아이콘 도구막대·원래 입력 초안 | 재사용 | 여섯 아이콘과 44px 누름 영역, 초안/수업 원문/권한 유지 |
| 상시 전체 도구막대 확대 | 배제 | 모바일의 판 공간과 버튼 밀도를 지키기 위해 나머지 도구는 클릭 메뉴 유지 |

### 이번 추가분의 검증

위 최종 실행 코드 검증에 통합했다. 초기 WebKit 성공/Chrome 실행 거부 이후, 동일한 Chrome 검수 요청이 정상 실행되어 20개 흐름을 통과했다. 이전 버전의 결과로 대신하지 않았다.
