# 화면·접근성 회귀 / 어휘 의미 기준 검수 — 2026-09-17

## 범위

PR #1323은 #1322 `e43d48aaf4b60396f98eb8f7885ba87fe459709d`에서 분리했다.
원래 iCloud 작업 공간의 변경은 보존했다. 운영 배포·고정 미리보기 별칭·DB·환경 변수·
교재 판본·복습 일정·개인 자료는 변경하지 않았다.

- 기존 실제 컴포넌트 E2E에 대표 화면 7종의 검토된 PNG 비교를 추가했다.
- Chromium/WebKit, OS/CPU/브라우저 버전별 기준을 분리했다.
- WCAG 자동 규칙과 키보드·초점·200% 본문 확대 검사를 추가했다.
- 이름 없는 버튼, 2px 이동을 주입하는 canary와 기준 이미지 승인 거부 검사를 추가했다.
- 뜻이 오른쪽 좁은 열에서 단어 중간에 끊기는 제품 결함을 수정했다.
- 문장 선택/단어 버튼 중첩, 15px 터치 영역, 화면 끝에서 잘리는 키보드 초점을 수정했다.
- 중·일 의미 기준 40건(문맥 32, 모호성 8)을 준비했다. AI 공급자를 호출하지 않았다.

## 증거의 구분

합성 DB·합성 사용자·합성 문장으로 실제 컴포넌트와 저장/복귀 경로를 검증한다.
스크린샷의 원어 주석/오른쪽 뜻과 기능 상태를 직접 확인한 뒤 이미지 SHA-256에 묶어
기준을 승인한다. CI가 기준 이미지를 자동 등록하지 않는다.

배포 폰트, 물리 iPad/Pencil, 실제 계정과 Gemini 인식 결과는 이번 자동 검수의 결론에
포함하지 않는다. SVG 대비 등 axe `incomplete`는 자동 통과로 바꾸지 않고 보고서에 남긴다.
의미 자료의 일본어 대응 후보에는 독립 이중언어 검토가 필요하다. 대만 사전 독음 자료를
HSK 대륙 표준 정답으로 사용하지 않는다.

## 로컬 최종 결과

실행 앱 코드: `c46664250b2ec7dc8b6b0c1e63ad3a9496e719b7`.

- 새 Next 합성 빌드 PASS(480개 정적 생성 경로, 기존 경고만). 운영 환경 파일 없이 실행했다.
- 실제 JSX를 포함한 변경 검사 PASS. 저장소 lint는 기존 익명 기본 export 경고 2건만 남았다.
- 뷰어 관련 77개 및 QA/의미 판정 12개 단위 검사 PASS.
- Chromium/WebKit 각각 수업 51개·개인 노트 11개 검사 PASS. 정리 완료, 브라우저 오류 0.
- 이 12개 화면은 승인 후 독립 재실행에서 모두 차이 0픽셀, 자동 접근성 위반 0.
- 별도의 일본어 검사도 두 엔진에서 10사례×8토글×2표현 방식·3폭 PASS, 기준 이미지 일치.
- 단어·문장 키보드 조작 분리, 닫기 초점 복귀, 실제 본문 2배 확대 후 가로 넘침 없음 확인.
- 합성 학생의 직접 단어 저장·복습 출처·교사 뜻 수정 반영·충돌·RLS 거부 경로를 포함했다.

로컬 증거: `/private/tmp/manabi-visual-verified/`, 일본어 증거:
`/private/tmp/manabi-visual-ja-final/`, `/private/tmp/manabi-visual-ja-webkit-final/`.
검토된 이미지·해시는 `e2e/visual-baselines/`에 있다.

## Linux 최종 결과

검토 기준 포함 코드: `c0bfbe125aa4fdb788dd546c07308ef2fbe684d7`.
GitHub PR 병합 검수 checkout: `9ee2ba1f0429c262c20ec4a785fc791808db87c9`.

- [CI 35223524186](https://github.com/wonchance-art/manabi/actions/runs/35223524186) 전체 SUCCESS.
- 425파일/4,542검사 통과, 기존 1skip. Next 빌드·콘텐츠·기존 smoke/학습 검사 통과.
- release 10단계 전부 PASS, 같은 입력의 재개에서 10단계 전부 증거 재사용 PASS.
- 수업 각 51, 노트 각 11, 일본어 대응 각 4, SQL 7+26검사 및 일본어 표시 검사 통과.
- Linux 화면 14개 모두 차이 0픽셀, 안정 캡처, 자동 접근성 위반 0.
- 모든 자원 정리 완료, 브라우저 오류와 외부 AI 요청 0. WebKit 종료도 정상 완료.
- [월드 회귀 35223524173](https://github.com/wonchance-art/manabi/actions/runs/35223524173) SUCCESS.
- Mac/Linux·Chromium/WebKit의 검토된 기준은 총 28개다.

합성 결과 artifact: `synthetic-qa-9ee2ba1f0429c262c20ec4a785fc791808db87c9`.
build/release/resume 입력 지문은 모두
`b84964d150edb9003134a6270a71bcb756343ef815782c707fa4da9d492e66e3`이며 검사 중 입력 불변을 확인했다.
로컬 보관: `/private/tmp/manabi-visual-linux-verified/`.

이 기록과 작업 보드의 후속 커밋은 실행 코드·검사 정의·기준 화면을 바꾸지 않는다.
PR 최종 40자 head와 해당 head의 원격 CI는 #150 완료 handoff를 정본으로 삼는다.

## 중간 실패도 보존

- c4666425 CI 35222030280: 전체 425파일/4,542검사 통과, 기존 1skip.
- 이 실행의 수업 화면 10개는 안정 캡처·자동 접근성 위반 0. 새 기준 부재를 실제로 거부했다.
- WebKit 정리에서 browser_cleanup_timeout이 발생해 후속 독립 검사를 중단했다.
  이 실행은 전체 성공이 아니다. 위 최종 재실행에서 전체 정상 종료를 확인했다.

- 최초 로컬 전체 단위 검사에서 월드 자료 로딩 suite 1건이 60초 시간 제한에 걸렸다.
  UI 빌드와 브라우저를 함께 실행하던 때이며 월드 파일이나 제한 시간을 변경하지 않았다.
- 초기 합성 빌드 1회는 중단했고 재시도했다. 중단한 실행을 성공 증거에 포함하지 않는다.
- 부모 브랜치의 합성 빌드를 이용한 초기 화면 캡처는 진단용이다. 제품의 뜻 조판 수정은
  새 빌드에서 다시 확인한다.
- 빌드 중 검수 스크립트가 변경된 실행은 `source_changed_during_qa`로 실패했다. 이 실행을
  QA 통과로 세지 않고, 이후 앱 코드 고정 빌드와 최종 UI 재실행으로 구분했다.
- WebKit 네이티브 dialog는 Tab 이동 중 브라우저 크롬을 거쳐 돌아온다. 최소 재현으로
  확인한 이 동작과 실제 뒤쪽 페이지로 초점이 새는 현상을 구분하도록 검사를 수정했다.
- 집중 모드/크기 전환의 중간 프레임은 완료 상태가 아니다. 고정 지연으로 무시하지 않고
  실제 투명도와 실제 글자 크기가 목표에 도달하는지 기다린 뒤 검사한다.
- 기존 중첩 버튼과 작은 터치 영역은 axe가 실제로 거부했다. 위반 이미지의 기준 승인은
  도구가 거부하며, 제품을 수정한 새 빌드의 오류 없는 화면만 승인했다.

자세한 계약과 승인 절차: [화면·의미 품질 기준](../visual-and-lexical-quality.md).
