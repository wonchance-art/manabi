# 수업·개인 노트 통합 출시 후보 · 2026-09-17 KST

## 범위와 기준

오너 승인 설계: [출시 준비 설계](../manabi-classroom-release-20260917.md).
작업 공간 `/private/tmp/manabi-classroom-release-20260917`, 브랜치 `codex/classroom-release-20260917`.
출발점 #1320 `46e1fca755eadca274d3dbb31dc014fa9751cd99`, main `efa44c508e862bc118518e9235008201f291341d`.
기존 iCloud 작업 공간과 #1316~#1320 브랜치를 변경하지 않았다.

| 포함 PR | 사용자 기능 |
|---|---|
| #1316 | 개인 노트에서 먼저 필기하고 나중에 미완료 표현 정리, 서재에서 이어 하기 |
| #1317 | 교사 설명판 비공개 계정 저장, 재접속, 충돌·오프라인·응답 유실 복구 |
| #1318 | 지난 설명판 페이지를 현재 수업으로 가져오기 |
| #1319 | 여러 표현·필기 중 필요한 부분 선택, 복사, 단일 실행 취소 |
| #1320 | 저장 날짜 전체/기간 탐색, 2개 병렬 읽기, 독음 표시 보완 |
| 이번 보완 | 일본어 대응 요청·캐시에 품사 포함, 선택 범위별 수동 조회 동의, 연결 검수 |

원본 PR은 운영 통합 확인 전 닫지 않는다. 이 후보는 main 대상 draft이며 운영 배포가 아니다.

## 이번 수정

- 일본어 대응의 사용자·단어·선택 뜻·품사·자형을 하나의 조회 범위로 사용한다. cache v2로 이전 품사 누락 결과를 재사용하지 않는다.
- 품사는 요청 데이터에 그대로 전달하고, 없는 품사는 null로 유지한다. 수업/노트 전체 본문은 추가 전송하지 않는다.
- 범위 변경 시 이전 요청 구독과 수동 조회 상태를 초기화한다. 늦은 응답은 새 선택에 나타나지 않으며 다른 단어/뜻/품사에 자동 요청하지 않는다.
- 사전 대응, 글자별 신자체, 현재 뜻의 일본어 표현을 구분하는 기존 화면은 유지한다. AI 공급자/저장 형식/교재·개인 뜻/복습 일정을 변경하지 않는다.
- 연결 화면 검수에서 수업 기록의 직접 입력 뜻과 학생 사본 뷰어의 자동 분석 뜻이 달라지는 문제를 발견했다. 분석·재시도 때 원문 줄과 전체 표현이 정확히 한 토큰에 대응하면 확정한 뜻·독음을 보존한다. 개인 교정(빈 독음 포함)이 우선이며, 문장 분할·모호한 앵커·원문 편집에는 억지로 연결하지 않는다. 공유 기준값을 개인 교정으로 표시하지 않아 사본 업데이트 규칙도 유지한다. 기존 자료 일괄 재분석은 실행하지 않는다.

## 검증과 증거 범위

- 공식 Node24.20.0: 전체 Vitest **417파일, 4,497개 PASS**, 선택 성능 측정 1개 skip. 신규 의미·범위 검사 51개 및 수업 뜻·독음 보존 회귀 검사 10개 포함.
- 변경 JS/JSX ESLint 및 `git diff --check` 통과.
- Next production build 통과. 기존 두 lint 경고 유지. 최초 실행은 기본 2GiB 힙 한도에 도달했고, `NODE_OPTIONS=--max-old-space-size=6144`로 완료했다. 공개 더미 Supabase 주소와 기존 Google font fixture로 로컬 빌드했다. 이 로컬 출력은 배포에 업로드하지 않는다.
- 실제 `ViewerJapaneseReference` + TanStack Query + HTTP fixture: Chromium/WebKit 각각 4동선 PASS. 품사만 변경해도 실제 fetch 취소, 늦은 응답 무시, 뜻별/계정별 캐시 분리, 새 선택의 명시적 재요청, 로그아웃, null 결과 후 재시도 확인.
- 개인 노트 Chromium/WebKit 각각 11동선 PASS: 두 차례 인식, 한자 후보 선택/직접 뜻 보존/중복 방지, 미완료·담음·제외/재접속, 부분 실패만 재저장, 원문 복귀, 저장 충돌·오프라인·응답 유실·백업, 기존 SRS 불변. 실제 React 화면과 로컬 SQL RPC, 합성 인식 응답을 사용한다.
- 교사→학생: `QA_CLASS_RELEASE=1`이 기존 설명판 E2E의 교사 UI가 실제 로컬 SQL에 남긴 결과를 같은 DB에서 읽는다. 별도 browser context/학생 ID, 실제 학생 화면·copy route·copy SQL·RLS·서명 검증을 사용한다. 인증/HTTP transport는 fixture이며 운영 토큰·실계정은 사용하지 않는다. 결과와 대표 화면은 최종 실행 후 아래에 기록한다.
- 교사와 학생을 전환할 때 교사 페이지의 요청을 먼저 종료하고, 학생 SQL은 transaction-local role/auth.uid를 사용한다. 무조건 허용하는 권한 mock으로 판정하지 않는다. 학생 수업 입장 HTTP 게이트는 서명/팀/세대 판정 fixture이며, 원격 Auth/팀 암호 입력 통합 확인과 구분한다.

합성 검수를 운영 로그인·실제 Gemini 인식 또는 물리 iPad/Pencil 검수로 세지 않는다. 서버 학생 사본→개인 어휘 저장·FSRS/출처는 기존 개별 검수 근거를 유지하며, 이번 교사→학생 연결 추가는 공개 기록→사본→뜻 확인→날짜 기록 복귀/재열기·비공개 권한까지다.

## 의미 사례 24개

정본: `e2e/fixtures/lexical-senses.json`. 중국어12/일본어8/영어2/프랑스어2.
원어 의미·독음은 링크된 교육부 사전, 小学館 사전의 공개 항목, Cambridge, Larousse에서 확인했다. 한국어 설명은 짧은 자체 요약이며 사전 예문/본문을 복제하지 않는다.

- `lexicalSenseContracts.test.js`: 다른 인식 결과가 현재 확인한 뜻/독음/언어/원문 ID를 덮지 않음, 미확인/제외 항목 저장 거부, 중국어에서 다른 뜻/품사의 일본어 사전 결과 재사용 거부.
- 이 테스트는 고정된 선택 결과의 보존·분리 계약이다. AI가 24개를 정확히 번역했다는 뜻이 아니다. **실제 공급자 표본 실행 0건**.
- `jaExample`은 검수용 편집 번역 후보이며 사전에서 그 정확한 대응어까지 검증한 값이 아니다. 공급자 품질 점수에 쓰지 않는다. 중국어 원어 의미 근거와 구분한다.
- 研究의 인용 독음 `yán jiù`는 대만 사전 기준이다. 대륙 HSK 독음 기준으로 쓰거나 기존 사용자 독음을 교체하지 않는다. 東道主는 손님을 맞는 주인 의미로 한정하며 문맥 없이 국가로 좁히지 않는다.
- 기존 #1320의 60개 표시 자료는 별도다. 이번 24개와 합쳐 어휘 정확도84건이라고 표시하지 않는다.

## 데이터 호환과 복귀

- #1320 기준 `teachingBoard.js`, `teachingBoardCloud.js`, `studyNotes.js`, 재사용 모델, 전체 migrations diff 0. 설명판/manifest/개인 노트 version 1 유지. 이번 추가 변경의 복귀 후보는 검증된 #1320 실행 배포다.
- main 대비 개인 노트는 summary 메타데이터가 추가되지만 기존 document/candidates 형식은 그대로다. 설명판 cloud는 신규 계정 저장 계층이며 main에 없던 기능이다.
- 따라서 옛 main 배포를 동일 기능의 안전한 복귀판이라고 주장하지 않는다. cloud 기능 없는 코드에서는 새 계정 판을 열 수 없으므로, 장애 시 #1320의 저장·복구 기능을 갖춘 버전을 우선 사용한다. 주소/코드 확인과 승인은 별도로 수행한다.
- 복귀는 웹 코드만 바꾼다. 설명판 테이블·비공개 버킷·신규 사용자 필기·개인 사본·단어장·복습 기록을 삭제/다운마이그레이션하지 않는다. DB/Storage 자동 수거는 범위 밖이다.
- 새로운 운영 DB 적용, 프로젝트 환경 변수 변경, 고정 alias 이동, 운영 병합/승격은 실행하지 않았다. #1317에서 이미 적용한 저장 계약을 재사용한다.

## 원격 결과

통합 PR·Preview·최종 실행 commit·CI·화면 검수 결과는 배포 완료 후 갱신한다.

## 실계정·물리 기기에서 남은 4개

1. 교사 계정: 기존 설명판에서 전체/부분 가져오기 → 저장 → 새 접속 복원.
2. 다른 학생 계정: 팀 암호 입장 → 당일/지난 수업 표현 → 개인 사본 → 뜻·독음 확인 → 단어 저장 → 출처 복귀. 교사 편집 UI가 없어야 한다.
3. 기존 개인 노트: 두 차례 선택 인식 → 직접 수정 → 나중에 정리 → 기존 단어 일정 보존 확인.
4. 실제 iPad: Pencil/손바닥 구분, 소프트 키보드가 열린 상태, 가로·세로 회전. 자동 WebKit 터치 검수와 구분한다.

사용자가 외출 중이므로 이번 실행 중 로그인 요청 없이 진행했다. 위 확인 전 운영 출시 완료로 보고하지 않는다.
