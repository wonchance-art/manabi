# 개인 노트 누적 정리 검수

2026-09-16. 기준 main efa44c508e862bc118518e9235008201f291341d. 독립 worktree `codex/note-collection-flow-20260916`에서 구현했으며 원래 공유 작업 폴더를 변경하지 않았다.

## 범위

인식 후 필기 화면 유지, 대기 개수 표시, 미완료/담음/제외/전체 필터, 간결한 저장 항목, 필기 계속하기, 서재의 미완료 이어 정리. 기존 API/DB/권한/AI 동의/원문/SRS를 사용한다. 상세 설계 `docs/manabi-note-collection-flow.md`.

## 단위·통합

- 최종 전체 Vitest 406파일/4,317개 PASS.
- 변경 핵심 모델·개인 노트·API·선택 인식·UI 규약 51개 PASS.
- 변경 JSX/JS 별도 lint 오류 0. 기존 LibraryShelf effect 의존성 경고 1개는 변경 전부터 존재한다. `git diff --check` PASS.
- 기존 계정 한정 노트 조회와 revision 조건 갱신은 수정하지 않았다. Supabase 변경 이력 및 maybeSingle 문서 확인. 새 SDK/schema/env 없음.

## 브라우저·배포

프로덕션 빌드 479페이지 PASS. Chromium/WebKit 각각 11흐름 PASS, 실행 오류 0. 누적 정리·제외/복원·재접속·서재 이어 정리를 포함한다. 기존 교사 설명판도 단독 재검수 13흐름 PASS, 실행 오류 0. 합성 계정과 폐기용 PostgreSQL이며 실제 개인 필기·외부 AI를 전송하지 않는다. 실제 iPad/Pencil 검수로 표시하지 않는다.

초기 브라우저 검수에서 서재 단계의 RPC 응답 fixture가 없고 새 책장 경로에 연결이 빠진 것을 발견했다. 새 책장·기존 도구 양쪽 연결, 원문을 가져오지 않는 계정 한정 요약 조회, 해당 RPC fixture를 보완해 Chrome/WebKit에서 전량 통과했다. 저장용 요약은 클라이언트가 전달한 수치 대신 서버에서 검증한 후보로 계산한다.


## 재검수 상세

- 새 CSS의 viewport 쿼리 증가는 UI 규약 검사에서 검출됐다. 기존 container 문맥을 사용하도록 고쳐 규약 검사와 최종 전체 테스트를 통과했다.
- 교사 회귀의 첫 동시 실행에서 테스트 입력이 메뉴 초기 초점보다 빨리 진행되어 다른 칸에 들어갔다. 해당 테스트는 실제 입력창에 초기 초점이 도착한 것을 확인한 뒤 입력하도록 보완했고 단독 실행에서 13흐름을 통과했다. 실행 앱의 교사 동작을 변경하지 않았다.
- 320/390px 정리 패널, 1440px 필기/정리 병행, 1024/768px 교사 화면 캡처를 직접 확인했다. 원래 필기·후보 수정·저장 선택·SRS 보존과 저장 응답 유실·연결 실패·충돌 회복도 검사했다.
- 실행 head 3942697dcf65d86ffeb9864eeb9ec0b46c17a819, PR #1316. Preview 2b7gr4lug, dpl_EFjRhCLaNFhmJiHM3zP3NDjVVvKh. 이후 커밋은 검수 도구/문서/자기 보드만 변경하며 앱 코드는 동일하다. 최종 원격 CI·Preview READY/버전·배포 재검수 결과는 PR/#150의 exact-head 완료 코멘트를 따른다.
- 운영 merge·승격·DB/schema/env·실제 개인 자료/단어/수업 기록 변경 없음. 물리 iPad/Pencil·실제 필체 추가 검수·설명판 서버 백업·공유 이미지 오류는 별도 후속이다.
