# 뷰어 문맥 뜻 선택 — 2026-09-18

## 문제와 결과

중국어 `东道主`는 같은 명사라도 대접하는 사람과 행사 주최 측을 가리킬 수 있다. 기존 분석은 같은 품사의 첫 사전 뜻을 고르므로 접대 문맥에 `주최국`이 남았다. 이번 변경은 분석을 무조건 덮어쓰지 않고, 단어창에서 학습자가 뜻을 비교하고 확정하는 흐름을 추가한다.

단어 → **다른 뜻 확인** → 사전 후보 비교 → 필요할 때 **이 문장의 뜻 확인** → AI 후보와 문맥 근거 확인 → **이 자료에만 저장**. 하단 개인 단어 저장/복습 평가는 그대로다. 사전 후보와 AI 후보를 구분하며 뜻을 고른 것만으로 저장하지 않는다. 일본어 대응어 조회는 저장된 뜻을 기준으로 새로 분리한다. 조회 결과가 도착해도 동작 버튼을 유지해 키보드 초점이 사라지지 않는다.

## 범위와 데이터 경계

- 중국어 본문 토큰의 개인 자료 소유자에게 자료 한정 저장을 제공한다. 다른 사람의 자료에서는 뜻 후보 비교만 가능하다. 수업의 별도 교사 편집기는 기존 흐름을 유지한다.
- 기존 토큰 교정/RPC 및 교정 기록 경로를 사용한다. 소유권과 현재 뜻을 확인하며, legacy UPDATE는 반영된 행이 없는 경우 실패로 처리한다.
- 사전 후보는 선택된 뜻과 명시된 품사만, AI 후보는 뜻만 전달한다. 독음·본문·출처·개인 단어·복습 일정·공유 사전에는 자동 반영하지 않는다.
- `/api/explain`의 명시적 후보 분기는 기존 인증/요청 제한을 사용한다. 모델 조회만으로 교정 기록을 포함한 DB 쓰기를 하지 않는다. 기존 문맥 해설 API 응답과 수업 해설은 유지한다.
- 계정·자료·토큰·표제어·표면형·현재 뜻·품사·전체 문장으로 후보 캐시를 분리한다. 자동 조회/재연결/무효화 재조회가 없고 선택 전환 시 진행 중 요청을 취소한다. 불확실하거나 형식이 맞지 않는 출력은 확정 후보로 내보내지 않는다. AI를 다시 조회하면 이전 AI 선택을 해제하며, 요청 중/실패 상태에서는 이전 응답의 후보를 숨긴다. 새 응답이 보류일 때 보이지 않는 이전 후보를 저장하는 경로도 차단한다.
- DB/Storage 스키마, 운영 환경 변수, 운영 배포와 main 병합은 변경하지 않는다.

## 검증 및 증거 구분

- 단위/API: 같은 품사의 다른 뜻, 필드 제한, 계정 변경, 전체 문맥 캐시, 불확실/잘못된 응답, 인증/입력 거부, 조회 시 DB 무기록, 기존 설명 API/일본어 회귀를 검증한다.
- 합성 브라우저: Chromium/WebKit에서 후보 조회가 수동임을 확인한다. 저장 실패 후 선택 유지, 성공 후 접힘/초점 복귀, 늦은 응답 취소, 계정/품사/뜻/문장 분리, 조회 버튼 초점 유지, 재시도/보류, 읽기 전용/로그아웃, 390/1180px 넘침을 검증한다. 실제 컴포넌트와 API 형식의 모의 응답을 사용하며 실제 DB 저장/외부 모델 품질의 증거로 간주하지 않는다.
- reader 프로필은 6단계, release 프로필은 12단계로 확장했다. 신규 시나리오도 필수 CI에 포함한다. JS/JSX ESLint는 `--ext .js,.jsx`로 검사한다.
- 화면을 직접 열어 390px Safari 캡처의 여백·현재 뜻/후보 구별·버튼 대비를 확인했고 언어 색상을 따르도록 저장 버튼을 보완했다.
- 최초 전체 테스트는 변경하지 않은 콘텐츠 대량 로딩 두 곳에서 시간 초과가 발생했다. 테스트 내용을 바꾸지 않고 동시 실행 수를 2로 낮춘 재검수에서 429파일/4,581개 PASS, 기존 1 SKIP을 확인했다. 최종 커밋 후 관련 56개와 변경 JS/JSX ESLint도 통과했다. 최종 결과/정확한 head/CI/배포는 #150 인계 및 로컬 closeout에 기록한다.

## 통합 화면 차이 재검토

앱 커밋 `d38b6d71e84c35ecaa0e78be3a66cc6a63ee0fc9`의 원격 CI35280813766은 빌드·기본/학습 동선·SQL·개인 노트·읽기 후보를 통과했지만 기존 단어창 기준 이미지와 달라 최종 gate를 실패 처리했다. Linux Chromium 2,701픽셀/WebKit 2,537픽셀, macOS Chromium 2,658픽셀/WebKit 2,694픽셀 차이를 이전/이후 캡처로 직접 대조했다. 모두 새 **다른 뜻 확인** 행과 그에 따른 본문 스크롤 영역 내 재배치였다. 단어·독음·뜻·일본어·하단 새 단어 저장/FSRS가 유지되고 겹침/잘림이 없다. 나머지 네 화면은 네 환경 모두 차이 0, 안정 캡처, 자동 접근성 위반 0이었다.

직접 본 `reader-inspector-phone` 네 이미지와 각각의 해시/검토 기록만 갱신한다. 비교 허용 오차(20픽셀), 탐지 canary, 필수 시나리오는 그대로 유지한다. 이는 실패 무시나 자동 스냅샷 승인이 아니며, 신규 기준으로 원격 통합 검수를 다시 실행한다. 이후 최종 상태 재검토에서 AI 재조회 시 이전 선택이 남는 경로를 보완했다. 최종 앱 실행 SHA/Preview와 최종 원격 검수는 #150 인계를 따른다.

## 남은 확인과 다음 방향

실계정 브라우저가 연결되지 않아 실제 공급자의 새 문맥 후보 → 자료 한정 저장 → 재접속 → 일본어 대응 흐름은 이 문서 시점에 미검수다. 정상 로그인된 자체 작성 검수 자료로 재개하며, 원본·개인 단어/SRS·공유 사전 불변까지 확인해야 한다. 기계적 스키마 검증은 번역의 정확성을 증명하지 않는다.

기존 legacy 자료의 전체 JSON UPDATE 경로는 다른 창의 동시 수정과 경합할 여지가 남아 있다. source-passage RPC의 비교 저장과 같은 수준으로 legacy 경로를 옮기는 일은 DB 변경을 포함하는 별도 제안이다. 이번 변경의 `expectedMeaning` 확인을 서버 동시성 보장으로 표현하지 않는다.

우선순위는 (1) 이번 기능의 실계정 의미/저장 검수, (2) 교사·학생 원본 직접 학습 흐름의 실제 권한 검수, (3) 이중언어 감수와 물리 iPad/Pencil이다. 의미 품질은 접대/행사/동형어 등 독립 표본별로 평가하며 임의 정확도 백분율을 제시하지 않는다.

## 실제 계정 후속 — 2026-09-19 KST

고정 QA의 a1949db9에서 기존 정상 로그인 세션과 앞서 자체 작성한 4문장만 사용했다.
Mac 잠금과 별개로 브라우저 연결이 복구되어 새 인증 정보 없이 검수했다. 원문·인증 토큰을
외부 검수 도구로 추출하지 않았으며, 아래 입력은 이전 기록에 있는 자체 작성 문장이다.

- `今天我请客，我来当东道主。`에서 사전 후보 `주최국`과 현재 자료 뜻을 구분했다.
- 실제 문맥 조회 1회는 `접대하는 사람 또는 주최 측`을 내놓았지만, 설명은 `请客`를 근거로
  대접하는 주인이라고 좁혔다. 후보와 설명의 의미 범위가 달라 **의미 검수 통과로 세지 않는다**.
- 후보 선택만으로 기존 뜻은 바뀌지 않았다. 자료 한정 저장 후 접힘·초점 복귀와 새로고침 후
  교정 유지, `dōng dào zhǔ` 유지, 공통 사전 후보 `주최국` 유지, 수집 수 2개 유지를 확인했다.
  단어 저장/채점은 누르지 않았다. 이 관찰을 전체 DB/SRS 불변 감사로 확대하지 않는다.
- 저장한 뜻에 대한 실제 일본어 조회 1회는 `主催者`를 반환했다. 버튼과 키보드 초점은 유지됐지만
  앞 단계의 넓은 뜻을 뒤따른 것이므로 접대 문맥의 정확한 대응으로 통과시키지 않았다.

의미 판단 근거는 [교육부 東道主 사전](https://dict.concised.moe.edu.tw/dictView.jsp?ID=9764&la=0&powerMode=0)의
손님을 맞는 주인 의미와 [일본어 主人 사전](https://dictionary.goo.ne.jp/word/%E4%B8%BB%E4%BA%BA/)의
손님을 맞아 대접하는 사람 용례다. 이 문장에서는 ‘주최 측’까지 함께 제안할 필요가 없다는 것이
이번 편집 판단이며, 모든 문맥에서 主催者가 틀렸다는 일반화가 아니다.

### 이번 보완과 검수 경계

문맥 후보 요청에서 기존 분석 뜻·품사를 모델에 전달하지 않고, 문장·단어·표면형만 인용 데이터로
전달한다. 문장 안의 행위자·대상·상황으로 한 쓰임을 고르고 다른 상황의 뜻을 묶지 않도록 지침과
무관한 단어의 대조 예를 보강했다. 모호하면 기존 `uncertain:true, meaning:null`로 보류한다.
기존 API 응답·저장·사전 조회·모델 티어는 바꾸지 않는다. 캐시 판본만 2로 분리한다.

이는 프롬프트 지침이며 의미를 기계적으로 보증하는 필터가 아니다. 한국어 ‘또는’를 일괄 금지하는
문자열 검사는 정상 정의까지 거부할 수 있어 넣지 않았다. 기존 의미 40사례 자료의 `zh11`에 이번
접대 문맥이 이미 있으므로 새 정답 집합이나 모델 평가 점수를 중복 생성하지 않는다.

관련 3파일/15개·전체 429파일/4,582개 PASS(기존1 SKIP), 변경 JS ESLint/diff 통과.
최종 reader/원격 CI/Preview 및 보완 후 실제 응답은 최종 head의 #150 인계와 로컬 closeout에
기록한다. 실제 AI 2회는 UI에서 누른 요청 수이며 공급자 내부 호출/모델명·독립 이중언어 감수와
구분한다. 교사/학생 팀 권한·물리 기기는 이번 일반 학습자 검수로 대체하지 않는다.


### 승인 후 재검수와 일본어 요청 분리 — 2026-09-19 KST

사용자가 특정 자체 검수 문장·단어·선택 뜻의 기존 Gemini 및 설정된 Groq 폴백
전송을 승인한 뒤 9ab60135에서 수동 문맥 조회와 일본어 조회를 각각 1회 실행했다.
이번 문맥 후보는 `접대하는 사람`이고, 근거도 `请客`의 손님 대접이라는 한 쓰임과
일치했다. 선택만으로 저장하지 않음 → 자료 한정 저장 → 새로고침 후 해당 뜻과
병음 유지 → 사전 후보 `주최국` 유지, 수집 수 2 유지를 확인했다.

일본어 응답은 `東道主`를 대응어로 표시하면서 ‘일본어에서는 거의 사용되지 않으며
보통 主催者나 ホスト를 뜻함’이라는 설명을 붙였다. 자형 표기와 실제 대응어를
혼동하는 결과이므로 이 단계는 실패로 기록했다. 조회 후 버튼과 초점은 유지됐다.

기존 조회 함수가 자형 변환 결과를 입력으로 보내고 번역과 자형 경고를 동시에
요구하던 부분을 보완한다. 모델에는 선택한 한국어 뜻·품사·참고 중국어만 보내고,
현대 일본어의 흔한 표현 하나(필요하면 짧은 구절)를 요청한다. 불확실하면 기존
null 보류를 사용한다. 자형 설명 생성은 요청하지 않고 응답에서도 받지 않는다.
UI의 기존 일본식 자형과 사전 경고는 유지하며, 캐시 v4로 이전 결과를 분리한다.
특정 단어의 정답을 하드코딩하거나 외부 공급자·모델 티어·DB 저장 경로를 바꾸지 않는다.

관련 5파일/82개 및 전체 429파일/4,583개 통과(기존 1개 skip), 변경 JS lint/diff 통과.
새 배포의 실제 일본어 결과는 최종 head #150/closeout에 기록한다. 프롬프트/목 응답
검사를 실제 모델 의미 정확성의 증거로 세지 않는다. 이 회차의 UI 요청 수 2회와
공급자 내부 호출 수는 구분한다. 정상 계정 식별자·인증정보·기존 개인 자료는
검수 기록에 포함하지 않았다.


브라우저 회귀의 첫 실행은 이전 AI 자형 설명이 나타나기를 기다리던 기대값에서
실패했다. 새 계약에 맞게 AI 자형 설명 부재·별도 자형 표시·사전의 기존 동형어 경고
보존을 확인하도록 기존 검사를 고쳤다. Chromium/WebKit reader 6단계 재실행 통과.
검사 강도를 낮추지 않고 수동 조회·취소·품사/계정별 캐시·초점·불확실 응답 복구도 유지한다.
