# 여러 표현 설명판 검수

검수일: 2026-09-15 KST. 작업: `codex/teaching-word-canvas-20260914`.
기준 main: `663288eba51310413e7e28107aac02e4cbd37044`.
구현·사용법: [여러 표현 설명판](../manabi-teaching-word-canvas.md).

## 자동 검사

- 전체 `npm test`: 394파일 / 4,220개 통과. 기존 테스트를 수정하지 않았다.
- 신규 회귀: 4파일 / 19개 통과. 좌표/표시8조합, 일본어 숙자훈, 긴 표현, 네이티브 글자 수정, 다른 뜻 구분, 저장 대기 구분, 인증·조회·AI 실패를 포함한다.
- `npm run lint`: 오류 0, 기존 `lessonModel.js`의 export 경고 2개.
- `npm run prebuild`: 콘텐츠 게이트 오류 0, 기존 경고 11개. 월드 생성 없음. 자동 갱신된 SW 버전만 복원했다.
- Next.js 프로덕션 빌드 통과. Node 24.20.0 공식 배포판, 테스트 전용 공개 키와 로컬 글꼴 fixture 사용. 실제 환경 파일/키를 읽지 않았다.

## 브라우저와 저장

`e2e/teaching-word-canvas.e2e.mjs`는 실제 React 화면에 합성 교사·교재를 연결하고, 수업 기록은 폐기 가능한 PGlite 데이터베이스의 기존 SQL RPC로 저장한다. 실제 개인 자료·운영 수업·FSRS를 변경하지 않는다. 사전/AI 응답은 성공·복수 뜻·지연·실패를 재현하도록 고정한다.

검수 동선:

1. 교재 뜻 창: 독음 위 / 글자별 훈음 아래 / 뜻 오른쪽, 교재 출처를 유지한 판 배치.
2. 네 표현과 필기가 같은 판에 공존. 판에 놓기만으로 학생 기록을 만들지 않음.
3. 다중 선택에서 카드/글자만·표시 변경, 가로 정렬·여백 줄이기. 단어별 뜻·출처와 필기를 보존.
4. 새로고침 후 장면 복원. 보여주기의 임시 가리기가 저장 원본에 영향을 주지 않음.
5. 다음 과로 이동한 뒤 이전 과에서 가져온 단어를 원래 자료 권한으로 확인하고 수업에 저장.
6. 여러 뜻 중 선택, 계속 추가, 입력 수정 후 늦게 도착한 조회 무시, 조회 실패 후 수동 입력.
7. 선택한 표현의 명시적 일괄 저장. 같은 표기의 다른 뜻 두 개 모두 저장하고 화면 안 오늘 표현 목록에서 확인.
8. 1440×1000, 1024×768, 768×1024, 390×844. 가로 넘침과 하단 주요 버튼 가시성 검사, 직접 이미지 검수.

최종 실행본의 로컬 및 배포 Chromium과 Safari 계열 WebKit + 터치에서 각각 13개 검수 항목을 통과했다. 실제 화면 오류 0. 메뉴 중 선택 버튼 숨김과 기존 드래그 표현의 뜻 편집·서버 저장 완료까지 확인했다.

검수 중 보완한 문제:

- 다중 표시 설정에 첫 단어의 전체 객체가 전달되어 다른 단어의 의미 메타데이터까지 덮어쓰던 문제를 표시 필드만 갱신하도록 수정했다.
- 글자별 훈음의 괄호 표기가 어색하게 나뉘던 줄바꿈을 개선했다.
- 모바일 알림이 하단 동작을 가리던 문제를 메뉴 안 알림과 고정 하단 동작으로 수정했다.
- WebKit의 원격 SVG 좌표 측정은 루트 경계를 반환할 수 있어, 실제 글리프의 DOM client rect와 캡처를 함께 검증한다.
- 여러 단어 드래그 후 기존 교사 패널의 뜻 편집에서 객체를 직접 렌더링할 수 있던 경로를 수정하고 브라우저 회귀 검사를 추가했다.
- 배포 WebKit 검사에서 `/home` 사전 로딩이 문서 새로고침으로 `cancelled` 되는 기록과, 배경 분석의 SQL `40001` 충돌 거부를 확인했다. 테스트는 정확히 이 원인이 확인된 전송만 별도 `expectedTransport`에 기록한다. 일반 409나 임의의 RSC 오류는 통과시키지 않는다. 모든 저장된 뜻·출처·서버 저장 완료를 끝까지 검사한다.

## 실제 교사 계정 검수 (2026-09-15 KST)

오너가 Codex 미리보기에 직접 로그인한 뒤 실행 `64a60be`를 검수했다. 합성 로그인이나 고정 조회 응답을 사용하지 않았다. 계정 식별자·개인 교재 본문·실제 계정 캡처는 이 문서나 공개 저장소에 추가하지 않는다.

- 팀 홈 → 읽던 중국어 교재 → 단어 뜻 → 설명판의 함께 보기 → 판 배치 → 팀 홈 복귀를 확인했다.
- 교재 단어 두 개와 입력 검색으로 찾은 교재 단어 한 개, 실제 AI 조회 표현 한 개를 같은 빈 판에 놓았다. 각각의 뜻·병음·훈음이 유지됐다. 교재 검색은 교재 출처를, 외부 표현 두 번의 실제 조회는 AI 설명 출처와 병음·한국어 뜻을 표시했다. AI 전체 품질·지연의 일반적인 보증으로 확대하지 않는다.
- 계속 추가 후 입력란이 비워지고 기존 표현들이 유지됨을 확인했다. 마우스 필기, 되돌리기·다시 실행, 네 표현 다중 선택, 카드/글자만 전환, 병음·훈음·뜻 독립 토글, 가로 정렬, 전체 보기를 확인했다. 필기는 표현 설정 변경으로 지워지지 않았다.
- 여러 표현과 필기를 함께 크게 보여주고 읽기·뜻을 일시적으로 가린 뒤 돌아왔다. 새로고침 후 원래 뜻·병음과 필기가 복원됐다.
- 1366×900, 1024×768, 390×844 뷰포트에서 메뉴를 직접 조작·캡처 확인했다. 기존 브라우저 확대 비율 때문에 마지막의 실제 CSS 폭은 354px이며 document clientWidth/scrollWidth 모두 354로 가로 넘침이 없었다. 주요 동작 버튼 44px, 하단 배치와 Tab 이동을 확인했다. 임시 뷰포트 설정은 해제했다.
- 현재 브라우저가 수집한 오류 로그 0. 판 배치·조회만으로 오늘 표현 0개가 바뀌지 않았다. 오늘 표현 목록과 서버 저장 상태를 읽기 확인했다. 실제 학생에게 검수 기록을 발행하거나 개인 복습을 채점하지 않았다. 서버 쓰기·중복 방지의 검증 근거는 위 격리 SQL/브라우저 검수이며, 실계정 쓰기 검수로 표기하지 않는다.
- 시작 시 판이 비어 있음을 확인했으며 종료 전 직접 추가한 네 표현·필기만 제거했다. 학생 수업 기록, 교재 주의점, 개인 단어·복습 일정은 수정하지 않았다.

## 출시 마무리 검수 (2026-09-15 KST)

- 오너가 줄바꿈 마무리·남은 검수·병합·운영 배포를 승인했다.
- 짧은 네 글자 표현은 오른쪽 뜻의 최소 공간을 확보할 수 있으면 한 줄로 유지한다. 긴 표현은 독음 묶음을 보존하면서 인접 줄의 길이를 균형 있게 조정한다. 6+1뿐 아니라 3+3+1 사례도 회귀 검사한다. 기존 저장 판은 자동 재조판하지 않는다.
- 실제 교사 계정으로 교재 표현 한 개를 오늘 수업에 저장했다. 저장 대기→서버 저장 확인됨, 새로고침 후 뜻/병음 유지, 기존 교재 버튼 비활성화, 설명판에서 동일 교재 표현 재요청 시 중복 거부를 확인했다. 운영 사이트의 수업 돌아보기에도 한 건이 나타났으며 교재 출처 버튼이 원문 단어와 뜻을 다시 열었다. 개인 단어 저장/복습 채점과 교재 주의점 편집은 하지 않았다.
- 이 승인된 실제 수업 표현 한 건은 삭제하지 않고 보존했다. 실제 학생의 별도 계정 로그인 검수로 확대하여 표기하지 않는다.
- 최종 커밋의 브라우저·CI·빌드 결과와 운영 반영 버전은 PR #1312 및 #150의 병합/배포 결과에 기록한다. 로컬 최초 빌드는 기본 2GiB 힙 한도에서 중단되어 로컬 빌드에만 4GiB를 허용했다. 운영 환경 변수 변경은 없다.
- 최종 실행 `cef2726bfde87b1daf239c7641e24e278272159e`의 로컬 프로덕션 빌드와 Vercel Preview `dpl_4FyFG8mw8sbKegR82kcfFWwCoZaf` 빌드가 완료됐다. https://manabi-aew9gob0x-wonchance-arts-projects.vercel.app 에서 Chromium/WebKit 터치 각각 13개 항목 통과, 화면 오류0. 네 글자 한 줄을 직접 캡처 확인했다. WebKit의 테스트 문서 교체 취소와 SQL revision 충돌은 앞서 설명한 두 건만 별도 기록했다.
- 최종 실행본과 동일한 소스의 CI34939392439에서 394파일/4,221테스트와 기존 smoke/learning-flow 모두 통과했다. 로컬 후속 검사는 재현 폭을 잘못 지정한 새 테스트 하나가 실패하여 폭을310px로 정정했고, 해당10개 재검사 및 위 전체 CI가 통과했다. 실행 실패를 숨기거나 불필요하게 기존 테스트의 기대값을 완화하지 않았다. 이후 변경은 검수 문서와 자기 보드뿐이다.

## 남은 미검수 범위

- 앞선 실계정 검수에서 확인한 줄바꿈 문제는 위 출시 마무리에서 보완했다.
- 물리 iPad/Pencil 필압, 손바닥 접촉, 실제 소프트 키보드, 학생 실시간 동기화는 이번 검수에 포함하지 않았다.

## 배포와 인계

- PR: https://github.com/wonchance-art/manabi/pull/1312 (draft)
- 최종 미리보기: https://manabi-ah6d78020-wonchance-arts-projects.vercel.app/class/culcom2
- 실행 커밋: `64a60be64da8da02be7528a26be9b3971f2cd6fb`
- Vercel: `dpl_6Xsdr5QQLAhsKZVJYzRp4ZBbztgw`, Preview, READY. `/api/version`의 전체 commit 일치.
- 실행본 CI: https://github.com/wonchance-art/manabi/actions/runs/34866544616 — lint/콘텐츠/전체 테스트 및 smoke/learning-flow 두 job SUCCESS.
- 이후 변경은 검사와 문서·자기 보드뿐이다. `src`, `public`, 설정과 의존성에 실행본 대비 차이가 없는지 확인한다. 최종 40자 head·CI는 #150 CODEX_DONE에 기록한다.

운영 병합·운영 배포·고정 alias·DB·환경 변수 변경은 하지 않았다.
