# 라이브 플레이 검수 리포트 — 게스트 하니스 1차 투어 (2026-07-22)

- 상태: 검수 완료분 정본화. 하니스·수정 PR은 전부 merge됨.
- 방법: dev 게스트 하니스로 실제 브라우저 플레이(스크린샷 실측). 오너 지시
  "게임 요소 전체 점검"의 라이브 축 — 정적 감사(#378·콘텐츠 밀도·도쿄 메모리)와 상보.

## 검수 하니스 (신규 — dev 전용, 제품 경로 불변)

| PR | 내용 |
|---|---|
| #388 | `NEXT_PUBLIC_WORLD_DEV_GUEST=1` + 비프로덕션일 때 /world 잠금 우회 |
| #389 | 게스트 멀티 effect 단락 — 오프라인 단독 입장(connected·기본 스폰) |
| #391 | `?spawn=` 직행: `air:<region>` / `city:<id>` / `<scene>@x,y` |
| #392 | dev에서 SW 등록 스킵 + 기존 등록·캐시 적극 해제 |

## 라이브로 검증된 것 (green)

1. **전국맵**: 렌더·이동·시간대(2077 클록)·NPC. 북한산 Ⓐ → 지식 카드 3층
   (factLine "삼각산" + 기념 스탬프 + desc) 정상.
2. **학습 루프(인천공항→하네다)**: 스토리 23p → 입국 심사 5문항 —
   타일 조립→역할 반전→빈칸(の)→응답(はい、そうです)→자유 작문(비채점·모범답 대조).
   산출 사다리 설계가 실플레이에서 그대로 작동. 통과 도장·출구 개방 연출 정상.
3. **리옹(v3 파일럿)**: `city:lyon` 진입 시 입국 브리핑(프랑스학 개관) 발동,
   terracotta 스킨·Part-Dieu 역 TRANSIT 프롬프트·가로등 프롭·저녁 조명.
   **mainRoute 포장 실렌더 확인**(테로 광장 남행 웜그레이 돌포장 띠) +
   엔진 산출 교차검증: 509 경로 타일·waypoint 오프셋 9·경로 프롭 20.
   Place des Terreaux Ⓐ 카드(사두마차 분수 — 무인명 수위) 정상.
4. **보르도**: Saint-Jean 역 스폰·terracotta·TRANSIT. **입국 브리핑 국가당 1회
   계약 검증**(리옹에서 소비 → 보르도 스킵).
5. **EMEA 오버월드**: 파리 CDG 도착 스폰·경계 표기 고지·센강·마을 프롭.
   제네바 노드 desc(제트 분수·시계 공방·국제기구) + **나라 오인 방지 계약 라이브 검증**
   — 딥링크가 fr-overview가 아닌 fr-francophonie(프랑코포니의 지도)로 연결.

## 발견·수정 완료 (이번 투어 산출)

- **#396 (P1·프로덕션)**: EMEA 자산 미들웨어 스테일 가드 — #306 일반 공개 후에도
  `/assets/overworld/europe-mediterranean-middle-east-*`를 관리자 전용 404로 차단,
  비관리자 전원이 유럽 지형을 받지 못했음. matcher를 `/admin/:path*`만 남기고
  테스트 신계약 갱신. (증상: "지역 지형을 불러오지 못했어요" + 404 `Vary: Cookie`)
- **#392 (dev DX)**: PWA SW가 dev에서 스테일 번들 서빙 → 검수 오진 2회 유발. 근본 차단.

## 발견·발주 완료 (Codex-1 E-큐, #150 5046785242)

- **E1**: 공항 스토리 씬 이탈 시 상태 잔존(오버월드에 텍스트박스·심사 진행,
  Ⓑ 불응 — 씬 컨텍스트 없는 진행 가능).
- **E2**: 심사 통과 '출구로' 소프트락 — 씬 전환이 좌표 저장 성공에 게이팅되어
  게스트/저장 실패 시 다이얼로그 영구 잔존. 로그인 유저도 API 일시 실패 시 동일 경로.
  (제네바 '들어가기'가 오버월드로 되돌아오는 현상도 동근 의심 — E2 수정 후 재검.)

## v3 리옹 파일럿 판정 (중간)

- 경로 위계 레이어(포장·이정표·가로등·경로 산출)는 **구현 계약대로 작동**.
- 다만 저녁 조명에서 포장 대비가 약해 "따라 걷고 싶은 길"의 시각 신호가 부족 —
  주간 재확인 후 야간 틴트 보정 또는 대비 강화 검토(다음 사이클).
- 남은 조각: 코스 카피·발견 이벤트 배선(Claude), 보르도·스트라스부르 확산 데이터(Codex-2 T3).

## 미검증 잔여 (다음 투어)

- 도시 진입 씬 전환(제네바 들어가기) — E2 수정 후 게스트로 재검.
- 레만/제네바 glacial 스킨·감천 파스텔 라이브 확인.
- 광장·에어허브 UI(게스트 도달 경로가 E2에 묶임).

## 운영 파인딩

- 세션 3·4가 iCloud vault 트리를 직접 checkout → 브랜치 전환이 dev 서버·에셋 서빙 파손.
  로컬 clone 의무화 발주(P0, #150 5046786117) + 보드 공지.
- zsh 글롭 다중 패턴이 첫 패턴 실패 시 전체 명령을 죽여 middleware.js 존재를 가림
  (`ls a.* b.*` 함정) — 진단 시 패턴별 분리 실행.
