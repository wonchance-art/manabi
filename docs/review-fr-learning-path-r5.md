# fr 학습 경로 R5 재판정 — landing (2026-07-30)

판정 질문(오너): **"챕터 1부터 끝까지 순서대로 배우면 충분한가?"**
대상: fr A1~A2 학습 경로 v1 (드릴 #697~701 · 누적 복습 #703 merge 이후 main `3aac491`).

## 1. 위임 감사 접지 대조 — 정량부 전면 기각 (스테일 트리)

위임 감사(백그라운드, 42 도구 호출)는 "불충분·배포 불가"를 판정했으나, 접지 증명 규격에 따른
발주자 재실측과 **전 항목 불일치**했다. 감사가 측정한 트리는 main이 아니다.

| 항목 | 감사 주장 | main 실측 (3aac491) | 판정 |
|---|---|---|---|
| a2_scenes.js | "존재하지 않음" | 실존 — 711행 · 6챕터 · 42문항 | 기각 |
| 드릴 보유 챕터 | 33~35개 | **51개** (13+7+3+2+12+6+6+2) | 기각 |
| 드릴 문항 | "quizItems 2챕터뿐" | **357문항** (fill 99 · choice 103 · order 50 · dictation 105) | 기각 |
| 딕테이션 | "0개 · 기획 메모만" | **105문항** + ChapterDrills 딕테 렌더러(RefSpeak TTS·dictNormalize) | 기각 |
| 누적 복습 | "로직 없음(검색 0건)" | `src/lib/learn/cumulativeReview.js` EVERY=5 + ReferenceChapterPage 배선(L614~617) | 기각 |
| wc -l | a1.js 995 등 | a1.js 1112 등 — 8파일 전부 상이 | 기각 |

측정법 주의: 문항 수는 `drills: [` 블록 **내부**의 `type:` 필드만 브래킷 매칭으로 계수(교차 오염 차단).

## 2. 콘텐츠 지적 개별 판정 — 치명 3건 전부 기각

| 감사 지적 | 실물 검증 | 판정 |
|---|---|---|
| "noix=견과류 오번역 ×5, 안전 위험" | 실물은 정반대 — `fruits à coque`=견과류로 가르치고 "noix는 기본적으로 호두" 경고 2회 명시(scene_travel L1513·L1542). noix 출현은 그 2곳뿐 | **기각** |
| "발음 챕터: parler l 발음 오류·s'il=si+il 아님·음절 수 혼동" | `parler` 발음 서술 자체가 파일에 부재. `s'il vous plaît`는 엘리종 예로 **정확히** 수록(L386 — si+il이 맞음, 감사 측 프랑스어 오류). 음절 분해(a·do·rable 3, in·tel·li·gent 4)는 전부 정확 | **기각** |
| "A1→A2 순서 역전(복합과거·명령법 선행 노출)" | P2 고정구 경로로 이미 처리 — scene_emergency "perdu는 … 이 문형 그대로 써요"(L441), 드릴 힌트 "(고정구)"(L326), Asseyez-vous는 직원 발화(수용 입력) | **기각** |
| "A2-01 과거분사 일치 누락" | a2.js L31에 선행 COD 일치 예고(la pomme que j'ai mang**ée**) + A2-02에서 être 성수 일치 전면 학습·드릴(a202-d1/d4) | **기각** |
| "여행 장면 IPA 37~39개 누락" | 대사 객체는 여러 줄 — 같은 줄 grep은 허수(발주자 1차 측정도 동일 함정). **객체 단위** 재계수: 228개 대사 전원 IPA 보유, 누락 **0** | **기각** |

## 3. 독립 재검증에서 발견·수리한 실결함 — a1_pronunciation.js 16건

감사와 무관하게 발주자 직접 전수에서 발견, 본 PR에서 수리:

- **IP 하드리밋 1**: 실존 인물 실명(Albert Einstein) 예시 → 일반명 예시로 교체
- **IPA 오기 5**: `le héros [lə ero]`×3 → `[lə eʁo]` / `et [ɛ]` → `[e]` / `[fo.to.gʁa.fi]` → `[fɔ.tɔ.gʁa.fi]`(L579와 정합)
- **사실관계 3**: 리에종 단어 목록의 `le` 오포함(le는 엘리종) 제거 / "전통적으로 …선택적 리에종" 방향 반전 정정(전통 규범=의무) / "h aspiré 형용사" → "h aspiré 명사"(héros는 명사)
- **카피 7**: 오타 2(울어난다·나뉨니다) / 발명 수치 "약 0.1초" 제거(P6) / titleFr 문법(se réveillent) / "demain의 첫음절 [dəmɛ̃]" 오기 / ZWNJ 유령문자 / 부자연 예문 "et enfant" → "et un enfant"×2(모순 IPA 동반 정정)

## 4. 3갭 최종 실측 (판정 근거)

| 갭 (R4 진단) | v1 실측 상태 |
|---|---|
| A. 연습량 | 51/51챕터 저작 드릴 357문항(챕터당 6~8) + buildChapterQuiz 3단 퀴즈 + RefPatternCheck 80% 관문 |
| B. 누적 복습 | 매 5번째 챕터 직전 4챕터 결정적 재출제(신규 저작 0·출처 라벨) + 문형 FSRS 카드 루프 |
| C. 듣기 | 딕테이션 105문항(TTS 재생→전사→관대/엄격 채점) — A1~A2 전 구간 분포 |

드릴 품질 게이트(전 라운드 확립): 비중복 3중(fr fail) · order 시점 준수 · id 챕터 접두 유일 — lint-curriculum (f) 상시 감시.

## 5. 최종 판정

**"순서대로 배우면 충분한가" — A1~A2 범위에서 충분(Yes).**
인풋(51챕터 위계) · 연습(357) · 누적 복습(결정적+FSRS) · 듣기(딕테 105) 4축이 실물로 배선·검증됐다.

정직한 잔여 격차(범위 외 선언):
1. **자유 산출(작문·발화) 피드백 없음** — 드릴은 인식·전사·배열까지. 자유 작문 채점은 v1 범위 외(샌드위치 자기평가·SRS 작문 카드가 부분 대체)
2. **실음원 없음** — Web Speech TTS만. 실음원 도입은 owner-gate
3. 어휘 커버 A1 94%·A2 98% (report-only 유지)
4. 진도 시각화 없음 — Codex-3 발주로 이관(draft·오너 승인 게이트)

## 6. 프로세스 교훈 (게이트 노트)

위임 감사 2건 연속 접지 실패: R4 전 1건(도구 0회 날조) · 이번 1건(스테일 트리 측정). 접지 증명 규격(발주자 재실측 대조)이 두 번 모두 오채택을 차단했다. 상시 규칙 추가:
- 위임 감사의 **첫 액션 = 대상 트리 `git rev-parse HEAD` 보고**, 발주자 HEAD와 불일치 시 즉시 중단
- 대사·중첩 객체 계측은 **객체 단위 파싱**만 인정(같은 줄 grep 금지 — 37건 허수 선례)
