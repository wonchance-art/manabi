import ClassTeamPage from '@/views/ClassTeamPage';

export const metadata = {
  title: '수업 자료',
  description: '팀 교재와 수업 정리 — 선생님께 받은 암호로 열어요.',
};

export default function Page() {
  return <ClassTeamPage />;
}
