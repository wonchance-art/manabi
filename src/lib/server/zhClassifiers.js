// 닫힌 양사(量詞) 목록 — v2-T R3의 유일한 열쇠.
//
// 양사는 **닫힌 부류**다(새 양사가 생기지 않는다). 그래서 열린 규칙(태그 추론)으로
// 잡으려던 R2 설계안을 버리고 목록으로 간다 — 지시사 계열은 태그 추론이 샌다는 것이
// T 설계 §6의 실측 결론이었다(`这样`·`那样` 오분리, `这首`·`这本` 놓침).
//
// 범위: HSK 1~4에서 실제로 쓰는 양사. 아래 넷은 **일부러 뺐다** — 양사보다 다른 쓰임이
// 압도적이라 오분리 위험이 이득을 넘는다(전부 실측으로 확인):
//   种(种类·各种) · 次(次数·其次) · 下(下午·下面) · 回(回来·回答)
// 넣고 뺄 때는 규칙 계약(zhQuantifier.test.js)의 오분리 표를 다시 돌린다.
export const ZH_CLASSIFIER = new Set([...'个本张条只件位双部家台辆支杯碗瓶把块片篇首节门场座段层份页间头棵封幅群排顿束朵副匹盒袋串']);

/** 지시사 — 양사 앞자리를 차지하는 셋. `哪`(의문)까지 같은 자리다. */
export const ZH_DEMONSTRATIVE = new Set(['这', '那', '哪']);
